{% extends '/admin/base.html.twig' %}

{% block title %}Admin - Modifier
{% endblock %}

{% block body %}
	<div class="container">
		<div class="d-flex flex-column justify-content-center align-items-center">
			<h1 class="text-center mt-5 mb-5">Modifier la catégorie "{{ category.name }}"</h1>
			<div class="d-flex flex-column">
				{{ include('/admin/admin_category/_form.html.twig', {'button_label': 'Modifier'}) }}

				<a href="{{ path('app_admin_category_index') }}">Retour à la liste des catégories</a>

				{{ include('/admin/admin_category/_delete_form.html.twig') }}
			</div>
		</div>
	</div>
{% endblock %}
