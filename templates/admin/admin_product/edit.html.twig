{% extends '/admin/base.html.twig' %}

{% block title %}Admin - Modifier
{% endblock %}

{% block body %}
	<div class="container">
		<div class="d-flex flex-column justify-content-center align-items-center">
			<h1 class="text-center mt-5 mb-5">Modifier le produit "{{ product.name }}"</h1>
			<div class="d-flex flex-column">
				{{ include('/admin/admin_product/_form.html.twig', {'button_label': 'Modifier'}) }}

				<a href="{{ path('app_admin_product_index') }}">Retour à la liste des produits</a>

				{{ include('/admin/admin_product/_delete_form.html.twig') }}
			</div>
		</div>
	</div>
{% endblock %}
